# 7c2ddbba-web-accessibility-wcag-debug
Accessibility bugs slip through your tests because WCAG violations hide in console logs and runtime warnings—scattered across a dozen browser tabs. The Web Accessibility (WCAG) Debug Dashboard and Log Analyzer VS Code Extension brings every accessibility issue into one centralized dashboard, right inside your editor.  Stop hunting for WCAG violatio
